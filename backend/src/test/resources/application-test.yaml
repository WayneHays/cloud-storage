spring:
  jpa:
    hibernate:
      ddl-auto: validate
  cache:
    type: none
minio:
  storage:
    bucket-name: test-bucket
    deletion-batch-size: 3
    connect-timeout: 2s
    read-timeout: 10s
    write-timeout: 10s
    call-timeout: 20s
bucket4j:
  enabled: false
